# Limite RDO

O código atual permanece temporariamente em `src/app/rdo` para evitar refatoração indiscriminada. Novas interfaces compartilhadas do RDO devem migrar para este limite por tickets próprios.
